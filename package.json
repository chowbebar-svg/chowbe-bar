{
  "name": "old-story",
  "version": "1.0.0",
  "description": "Old Story Whiskey Bar HTML template replica",
  "scripts": {
    "start": "npx --yes serve . -l 3000",
    "dev": "npx --yes live-server --port=3000 --open=index.html"
  }
}
